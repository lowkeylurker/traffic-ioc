{
  "name": "traffic-ioc-monorepo",
  "private": true,
  "version": "1.0.0",
  "packageManager": "pnpm@11.20.0",
  "scripts": {
    "dev": "turbo dev",
    "dev:admin": "turbo dev --filter=@traffic-ioc/admin-web",
    "dev:user": "turbo dev --filter=@traffic-ioc/user-web",
    "dev:backend": "turbo dev --filter=@traffic-ioc/backend",
    "dev:rag": "cd rag-ingestion && uv run uvicorn src.main:app --host 0.0.0.0 --port 8001 --reload",
    "format:rag": "cd rag-ingestion && uv run ruff format .",
    "lint:rag": "cd rag-ingestion && uv run ruff check --fix .",
    "typecheck:rag": "cd rag-ingestion && uv run pyrefly check",
    "check:rag": "cd rag-ingestion && uv run pyrefly check",
    "build": "turbo build",
    "build:check": "turbo build:check",
    "lint": "turbo lint",
    "prisma:gen": "turbo prisma:gen",
    "prisma:migrate:oltp": "pnpm --filter=@traffic-ioc/backend prisma:migrate:oltp",
    "prisma:pull:dw": "pnpm --filter=@traffic-ioc/backend prisma:pull:dw",
    "clean": "turbo clean && rm -rf node_modules apps/*/node_modules apps/*/dist apps/*/.next packages/*/node_modules packages/*/dist"
  },
  "devDependencies": {
    "prettier": "^3.1.1",
    "turbo": "^2.4.4",
    "typescript": "^5.3.3"
  }
}
