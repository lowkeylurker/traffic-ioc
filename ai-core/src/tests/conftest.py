"""
\"\"\"\nconftest.py - Cấu hình Pytest & Fixtures

Cung cấp:
- fixture: sample_traffic_data (dữ liệu giao thông mẫu)
- fixture: mock_db_session (phiên database giả)
- fixture: mock_forecasting_models (mô hình dự báo giả)
- fixture: mock_rl_agent (agent RL giả)
- fixture: mock_clusterer (clusterer giả)

Được chia sẻ qua tất cả test modules.
Sử dụng pytest, pytest-mock.
\"\"\"

import pytest

# TODO: Triển khai pytest fixtures
